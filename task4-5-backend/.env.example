# =============================================
#  Environment Variables — copy to .env and fill in real values
#  Task 4 & 5 - SaiKet Systems Internship
#  (.env is git-ignored — never commit real secrets)
# =============================================

# Server port (Render sets this automatically in production — only used locally)
PORT=5000

# Neon connection string: Neon dashboard → your project → Connection Details
# Use the POOLED connection string (hostname contains "-pooler") for Render.
DATABASE_URL=postgresql://neondb_owner:npg_1X7SaMVynTRD@ep-calm-credit-azcnl4lf-pooler.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
# Any long random string — used to sign JWTs. Generate one with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=9a2848c8901f7960e3edc10b01f1fae06b21903d50c0825a0deaace58fe8993c

# Comma-separated list of allowed CORS origins
FRONTEND_URL=http://localhost:5173,https://your-app.vercel.app
